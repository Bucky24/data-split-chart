{
	"version": "0.0.2",
	"name": "data-split-chart",
	"author": "rwijtman",
	"main": "build/main.js",
	"scripts": {
		"postinstall": "npm run babel",
		"babel": "babel --presets=react,env *.js --out-dir build"
	}
}
